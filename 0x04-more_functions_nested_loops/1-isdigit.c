#include "main.h"
/**
 * _isdigit - funtion to check for a digit (0 through 9)
 * @c: Variable text
 * Return: (1) if true and (0) if false
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
